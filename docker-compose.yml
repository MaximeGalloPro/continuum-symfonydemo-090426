services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "3003:3000"  # NestJS API (Swagger: http://localhost:3003/api-docs)
    volumes:
      - .:/app
      - /app/node_modules  # Volume anonyme pour isoler node_modules
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/app?schema=public
    depends_on:
      db:
        condition: service_healthy
    stdin_open: true
    tty: true
    command: ["npm", "run", "dev"]  # Démarre NestJS automatiquement

  frontend:
    build:
      context: ./frontend
      target: dev
    ports:
      - "5174:5173"  # Vite dev server (http://localhost:5174)
    volumes:
      - ./frontend:/app
      - /app/node_modules  # Volume anonyme pour isoler node_modules
      - ./frontend/screenshots:/app/screenshots  # Screenshots persistés
    environment:
      - VITE_API_URL=http://app:3000
    depends_on:
      - app
    stdin_open: true
    tty: true
    command: ["npm", "run", "dev"]  # Vite dev server (Playwright reuseExistingServer=true)

  db:
    image: postgres:16-alpine
    # No ports exposed - app connects internally via service name
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: app
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
